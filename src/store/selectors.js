export const selectBreweries = (state) => state.breweries;

export const selectIsLiked = (state) => state.isLiked;

export const selectFavorite = (state) => state.favorite;

export const selectCurrentFactory = (state) => state.currentFactory;
